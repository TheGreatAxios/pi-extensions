{
	"name": "pi-container-sandbox",
	"private": true,
	"version": "0.1.1",
	"description": "pi coding-agent extension that runs every read/write/edit/bash op inside a per-session Linux container (Apple `container` or Docker).",
	"type": "module",
	"scripts": {
		"build": "bun run build-image",
		"build-image": "which container >/dev/null 2>&1 && container build -t pi-sandbox:latest -f docker/Dockerfile docker || docker build -t pi-sandbox:latest -f docker/Dockerfile docker",
		"build-image:docker": "docker build -t pi-sandbox:latest -f docker/Dockerfile docker",
		"build-image:apple": "container build -t pi-sandbox:latest -f docker/Dockerfile docker",
		"clean": "echo 'nothing to clean'",
		"check": "echo 'nothing to check'"
	},
	"pi": {
		"extensions": [
			"./index.ts"
		]
	},
	"dependencies": {
		"@mariozechner/pi-coding-agent": "^0.70.2"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"typescript": "5.6"
	}
}
